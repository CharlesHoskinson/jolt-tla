\* TLC Configuration for MC_BadChain_Skip.tla
\* Non-vacuity test: expects TLC to find INV_ATK_NoSkipChunk violation
\*
\* Run: java -jar tla2tools.jar -config BadChain_Skip.cfg tla/MC_BadChain_Skip.tla
\* Expected: TLC exits with error (invariant violation)

SPECIFICATION SkipBadSpec

\* Inherit all constants from Jolt.cfg
CONSTANTS
    CHUNK_MAX_STEPS = 2
    MAX_CHUNKS = 3
    NUM_REGISTERS = 32
    FR_MODULUS = 1000000
    FR_TLC_BOUND = 131072
    U64_TLC_BOUND = 131072
    PAGE_INDEX_TLC_BOUND = 4
    MAX_DIRTY_PAGES = 2
    MAX_ABSORPTIONS = 4
    STEP_FN <- ModelStepFn
    ENTRY_POINT = 4096
    RW_REGION_BASE = 0
    RW_REGION_SIZE = 65536
    IO_REGION_BASE = 65536
    IO_REGION_SIZE = 8192
    DefaultHashes <- ModelDefaultHashes
    TAG_STRINGS <- SMOKE_TAG_STRINGS
    PoseidonHashBytes <- ModelPoseidonHashBytes
    PoseidonHashFr2 <- ModelPoseidonHashFr2
    SHA256Hash <- ModelSHA256Hash

\* The invariant we expect to be violated
INVARIANT CheckSkipDetected
