\* TLC Configuration for MC_BadRegistry_ExternalHandle.tla
\* Pattern A test: verifies external handles are blocked
\*
\* Run: java -jar tla2tools.jar -config BadRegistry_ExternalHandle.cfg tla/MC_BadRegistry_ExternalHandle.tla
\* Expected: TLC succeeds (invariant holds - external handles blocked by type system)

SPECIFICATION ModelSpec

\* Inherit all constants from Jolt.cfg
CONSTANTS
    CHUNK_MAX_STEPS = 2
    MAX_CHUNKS = 3
    NUM_REGISTERS = 32
    FR_MODULUS = 1000000
    FR_TLC_BOUND = 131072
    U64_TLC_BOUND = 131072
    PAGE_INDEX_TLC_BOUND = 4
    MAX_DIRTY_PAGES = 2
    MAX_ABSORPTIONS = 4
    STEP_FN <- ModelStepFn
    ENTRY_POINT = 4096
    RW_REGION_BASE = 0
    RW_REGION_SIZE = 65536
    IO_REGION_BASE = 65536
    IO_REGION_SIZE = 8192
    DefaultHashes <- ModelDefaultHashes
    TAG_STRINGS <- SMOKE_TAG_STRINGS
    PoseidonHashBytes <- ModelPoseidonHashBytes
    PoseidonHashFr2 <- ModelPoseidonHashFr2
    SHA256Hash <- ModelSHA256Hash

\* The invariant should hold (external handles blocked)
INVARIANT TestInvariant
